// N = 8 queens, 8 columns, 8 rows
export const N = 8;

// Column labels
export const COLS = ['a', 'b', 'c', 'd', 'e', 'f', 'g', 'h'];

// Default starting position (0-indexed rows) from the original project
// Corresponds to: a7, b6, c4, d8, e2, f3, g5, h1
export const DEFAULT = [6, 5, 3, 7, 1, 2, 4, 0];
